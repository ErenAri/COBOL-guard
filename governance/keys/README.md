# Governance Keys

Approved signer public keys live in this directory.

Use:
- `gm keygen --name <id>` to generate a key pair.
- Keep `<id>.private.pem` secure and out of source control.
- Commit only `<id>.public.pem` for CI verification.
